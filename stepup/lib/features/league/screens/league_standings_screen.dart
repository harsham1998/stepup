import 'package:flutter/material.dart';
class LeagueStandingsScreen extends StatelessWidget {
  const LeagueStandingsScreen({super.key});
  @override
  Widget build(BuildContext context) => const Scaffold(body: Center(child: Text('Standings')));
}
