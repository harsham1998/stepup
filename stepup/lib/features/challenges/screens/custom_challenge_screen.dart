import 'package:flutter/material.dart';
class CustomChallengeScreen extends StatelessWidget {
  const CustomChallengeScreen({super.key});
  @override
  Widget build(BuildContext context) => const Scaffold(body: Center(child: Text('Create Challenge')));
}
