import 'package:flutter/material.dart';
class RivalsScreen extends StatelessWidget {
  const RivalsScreen({super.key});
  @override
  Widget build(BuildContext context) => const Scaffold(body: Center(child: Text('Rivals')));
}
